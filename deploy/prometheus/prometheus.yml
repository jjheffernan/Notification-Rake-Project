global:
  scrape_interval: 15s

scrape_configs:
  - job_name: dashboard
    metrics_path: /health
    static_configs:
      - targets: ["dashboard:8000"]

  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
